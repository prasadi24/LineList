@using LineList.Cenovus.Com.Domain.DataTransferObjects.InsulationDefaultColumn
@model IEnumerable<InsulationColumnDisplayDto>

<div class="mt-md-0">
    <div class="row">
        <div class="card mb-md-1">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center">
                    <button class="btn btn-primary" id="btnCreateNewColumn">
                        <i class="fas fa-plus"></i> Add New Column
                    </button>
                 
                </div>
            </div>
            <div class="table" style="max-height: 400px; overflow-y: auto; border: 1px solid #ddd;">
                <table id="dataTableColumn" class="table table-bordered">
                    <thead class="sticky-table-header">
                        <tr>
                            <th>Actions</th>
                            <th>@Html.DisplayNameFor(model => model.First().RowNum)</th>
                            <th>@Html.DisplayNameFor(model => model.First().MinOperatingTemperature)</th>
                            <th>@Html.DisplayNameFor(model => model.First().MaxOperatingTemperature)</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr data-id="@item.Id" data-insulationdefaultid="@item.InsulationDefaultId">
                                <td>
                                    <button class="btn btn-sm btn-outline-primary btn-update-temp" data-id="@item.Id">
                                        <i class="fas fa-file-import"></i>
                                    </button>
                                </td>
                                <td>@item.RowNum</td>
                                <td>@item.MinOperatingTemperature</td>
                                <td>@item.MaxOperatingTemperature</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" id="btnCancelEditRowColumn">Close</button>
    </div>
</div
